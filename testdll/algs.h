#ifndef ALGS_H_INCLUDED
#define ALGS_H_INCLUDED

#include <cstdint>
#include <cstdlib>

uint8_t *bstrstr(uint8_t *data, uint8_t *pattern, size_t data_len, size_t pattern_len);

#endif // ALGS_H_INCLUDED
