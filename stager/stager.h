#ifndef STAGER_H_INCLUDED
#define STAGER_H_INCLUDED

/// This file is for the functionality that the
/// stager needs to export to other dlls

// cmd registration
// writing to pipe

#include <windows.h>

#endif // STAGER_H_INCLUDED
